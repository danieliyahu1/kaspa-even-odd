{
  "name": "kaspa-even-odd",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Non-custodial Even/Odd game protocol for Kaspa testnet-10",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test \"test/*.test.js\"",
    "check": "node --check src/index.js && node --check src/server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
